# How to push on your own branch

git checkout mbybranch
git status (to see if your branch is up to date)
git add .
git commit -m "My commit message"
git push


# Two choices :

By github : check the PR and merge
By CLI : git checkout main && git merge mybranch && git push origin main